# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# carga histórica (scripts/sync-historico.ts) — estado local de progresso, não é código
scripts/.sync-historico-progresso.json
.caed_session
# dados brutos extraídos do CAEd (nome de aluno — PII), nunca commitar
scripts/caed_*.jsonl

.anosfinais_session
# dados brutos extraídos do portal Anos Finais (MEC/CAEd), nunca commitar
scripts/anosfinais_*.jsonl

# cache de planilhas brutas do SAEB/INEP (arquivos nacionais grandes)
saeb_cache/
